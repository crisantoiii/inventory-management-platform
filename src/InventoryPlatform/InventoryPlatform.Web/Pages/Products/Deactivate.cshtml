@page "{id:int}"
@model InventoryPlatform.Web.Pages.Products.DeactivateModel

<h1>Deactivate Product</h1>

<div class="alert alert-warning">
    <strong>Warning!</strong>
    This product will become inactive and will no longer
    be available for new transactions.
</div>

<dl class="row">
    <dt class="col-sm-3">SKU</dt>
    <dd class="col-sm-9">@Model.Product.Sku</dd>

    <dt class="col-sm-3">Name</dt>
    <dd class="col-sm-9">@Model.Product.Name</dd>

    <dt class="col-sm-3">Status</dt>
    <dd class="col-sm-9">
        @(Model.Product.IsActive ? "Active" : "Inactive")
    </dd>
</dl>

<form method="post">

    <input asp-for="Request.Id" type="hidden" />

    <button type="submit" class="btn btn-danger">
        Deactivate
    </button>

    <a asp-page="Index" class="btn btn-secondary">
        Cancel
    </a>

</form>