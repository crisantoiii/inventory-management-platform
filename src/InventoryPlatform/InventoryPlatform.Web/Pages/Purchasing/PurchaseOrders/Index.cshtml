@page
@model InventoryPlatform.Web.Pages.Purchasing.PurchaseOrders.IndexModel

@{
    ViewData["Title"] = "Purchase Orders";
}

<div class="container-fluid">
    <div class="d-flex justify-content-between align-items-center mb-4">
        <div>
            <h1 class="h3 mb-1">Purchase Orders</h1>
            <p class="text-muted mb-0">
                View and manage purchase orders.
            </p>
        </div>

        <a asp-page="./Create" class="btn btn-primary">
            Create Purchase Order
        </a>
    </div>

    @if (TempData["SuccessMessage"] is string successMessage)
    {
        <div class="alert alert-success" role="alert">
            @successMessage
        </div>
    }

    <div asp-validation-summary="ModelOnly"
         class="text-danger mb-3"></div>

    <form method="get" class="row g-2 mb-4">
        <div class="col-md-4">
            <label for="search" class="form-label">Search purchase orders</label>
            <input name="Search"
                   value="@Model.Request.Search"
                   class="form-control"
                   placeholder="Search by purchase order ID or supplier"
                   autocomplete="off" />
        </div>

        <div class="col-md-2">
            <label for="FromDate" class="form-label">From date</label>
            <input name="FromDate"
                   value="@Model.Request.FromDate?.ToString("yyyy-MM-dd")"
                   type="date"
                   class="form-control" />
        </div>

        <div class="col-md-2">
            <label for="ToDate" class="form-label">To date</label>
            <input name="ToDate"
                   value="@Model.Request.ToDate?.ToString("yyyy-MM-dd")"
                   type="date"
                   class="form-control" />
        </div>

        <div class="col-md-2">
            <label for="status" class="form-label">Status</label>
            <select name="Status"
                    class="form-select"
                    asp-items="Model.StatusOptions">
                <option value="">All statuses</option>
                @foreach (var option in Model.StatusOptions.Skip(1))
                {
                    <option value="@option.Value"
                            selected="@(Model.Request.PurchaseOrderStatus?.ToString() == option.Value)">
                        @option.Text
                    </option>
                }
            </select>
        </div>

        <div class="col-md-2 d-flex align-items-end">
            <div class="col-auto me-2">
                <button type="submit" class="btn btn-primary">
                    Filter
                </button>
            </div>

            @if (!string.IsNullOrWhiteSpace(Model.Request.Search) ||
                 Model.Request.FromDate.HasValue ||
                 Model.Request.ToDate.HasValue ||
                 Model.Request.PurchaseOrderStatus.HasValue)
            {
                <div class="col-auto">
                    <a asp-page="./Index"
                       class="btn btn-outline-secondary">
                        Reset
                    </a>
                </div>
            }
        </div>
    </form>

    @if (!Model.PurchaseOrders.Items.Any())
    {
        <div class="alert alert-info">
            No purchase orders found.
        </div>
    }
    else
    {
        <div class="card">
            <div class="card-body p-0">
                <div class="table-responsive">
                    <table class="table table-hover align-middle mb-0">
                        <thead>
                            <tr>
                                <th>
                                    <a asp-page="./Index"
                                       asp-route-Search="@Model.Request.Search"
                                       asp-route-FromDate="@Model.Request.FromDate?.ToString("yyyy-MM-dd")"
                                       asp-route-ToDate="@Model.Request.ToDate?.ToString("yyyy-MM-dd")"
                                       asp-route-Status="@Model.Request.PurchaseOrderStatus"
                                       asp-route-SortBy="@IndexModel.SortFields.Id"
                                       asp-route-Descending="@(Model.Request.SortBy == IndexModel.SortFields.Id ? !Model.Request.Descending : false)"
                                       asp-route-PageNum="1"
                                       asp-route-PageSize="@Model.Request.PageSize"
                                       class="text-decoration-none">
                                        ID
                                        @if (Model.Request.SortBy == IndexModel.SortFields.Id)
                                        {
                                            <span aria-hidden="true">@(Model.Request.Descending ? "↓" : "↑")</span>
                                        }
                                    </a>
                                </th>
                                <th>
                                    <a asp-page="./Index"
                                       asp-route-Search="@Model.Request.Search"
                                       asp-route-FromDate="@Model.Request.FromDate?.ToString("yyyy-MM-dd")"
                                       asp-route-ToDate="@Model.Request.ToDate?.ToString("yyyy-MM-dd")"
                                       asp-route-Status="@Model.Request.PurchaseOrderStatus"
                                       asp-route-SortBy="@IndexModel.SortFields.Supplier"
                                       asp-route-Descending="@(Model.Request.SortBy == IndexModel.SortFields.Supplier ? !Model.Request.Descending : false)"
                                       asp-route-PageNum="1"
                                       asp-route-PageSize="@Model.Request.PageSize"
                                       class="text-decoration-none">
                                        Supplier
                                        @if (Model.Request.SortBy == IndexModel.SortFields.Supplier)
                                        {
                                            <span aria-hidden="true">@(Model.Request.Descending ? "↓" : "↑")</span>
                                        }
                                    </a>
                                </th>
                                <th>
                                    <a asp-page="./Index"
                                       asp-route-Search="@Model.Request.Search"
                                       asp-route-FromDate="@Model.Request.FromDate?.ToString("yyyy-MM-dd")"
                                       asp-route-ToDate="@Model.Request.ToDate?.ToString("yyyy-MM-dd")"
                                       asp-route-Status="@Model.Request.PurchaseOrderStatus"
                                       asp-route-SortBy="@IndexModel.SortFields.OrderDate"
                                       asp-route-Descending="@(Model.Request.SortBy == IndexModel.SortFields.OrderDate ? !Model.Request.Descending : false)"
                                       asp-route-PageNum="1"
                                       asp-route-PageSize="@Model.Request.PageSize"
                                       class="text-decoration-none">
                                        Order Date
                                        @if (Model.Request.SortBy == IndexModel.SortFields.OrderDate)
                                        {
                                            <span aria-hidden="true">@(Model.Request.Descending ? "↓" : "↑")</span>
                                        }
                                    </a>
                                </th>
                                <th>
                                    <a asp-page="./Index"
                                       asp-route-Search="@Model.Request.Search"
                                       asp-route-FromDate="@Model.Request.FromDate?.ToString("yyyy-MM-dd")"
                                       asp-route-ToDate="@Model.Request.ToDate?.ToString("yyyy-MM-dd")"
                                       asp-route-Status="@Model.Request.PurchaseOrderStatus"
                                       asp-route-SortBy="@IndexModel.SortFields.Status"
                                       asp-route-Descending="@(Model.Request.SortBy == IndexModel.SortFields.Status ? !Model.Request.Descending : false)"
                                       asp-route-PageNum="1"
                                       asp-route-PageSize="@Model.Request.PageSize"
                                       class="text-decoration-none">
                                        Status
                                        @if (Model.Request.SortBy == IndexModel.SortFields.Status)
                                        {
                                            <span aria-hidden="true">@(Model.Request.Descending ? "↓" : "↑")</span>
                                        }
                                    </a>
                                </th>
                                <th class="text-end">
                                    <a asp-page="./Index"
                                       asp-route-Search="@Model.Request.Search"
                                       asp-route-FromDate="@Model.Request.FromDate?.ToString("yyyy-MM-dd")"
                                       asp-route-ToDate="@Model.Request.ToDate?.ToString("yyyy-MM-dd")"
                                       asp-route-Status="@Model.Request.PurchaseOrderStatus"
                                       asp-route-SortBy="@IndexModel.SortFields.TotalAmount"
                                       asp-route-Descending="@(Model.Request.SortBy == IndexModel.SortFields.TotalAmount ? !Model.Request.Descending : false)"
                                       asp-route-PageNum="1"
                                       asp-route-PageSize="@Model.Request.PageSize"
                                       class="text-decoration-none">
                                        Total
                                        @if (Model.Request.SortBy == IndexModel.SortFields.TotalAmount)
                                        {
                                            <span aria-hidden="true">@(Model.Request.Descending ? "↓" : "↑")</span>
                                        }
                                    </a>
                                </th>
                                <th class="text-end">Actions</th>
                            </tr>
                        </thead>

                        <tbody>
                            @foreach (var purchaseOrder in Model.PurchaseOrders.Items)
                            {
                                <tr>
                                    <td>
                                        @purchaseOrder.Id
                                    </td>

                                    <td>
                                        @purchaseOrder.SupplierName
                                    </td>

                                    <td>
                                        @purchaseOrder.OrderDate.ToString("MMM dd, yyyy")
                                    </td>

                                    <td>
                                        <span class="badge bg-secondary">
                                            @purchaseOrder.Status
                                        </span>
                                    </td>

                                    <td class="text-end">
                                        @purchaseOrder.TotalAmount.ToString("N2")
                                    </td>

                                    <td class="text-end">
                                        <a asp-page="./Details"
                                           asp-route-id="@purchaseOrder.Id"
                                           asp-route-Search="@Model.Request.Search"
                                           asp-route-FromDate="@Model.Request.FromDate"
                                           asp-route-ToDate="@Model.Request.ToDate"
                                           asp-route-Status="@Model.Request.PurchaseOrderStatus"
                                           asp-route-SortBy="@Model.Request.SortBy"
                                           asp-route-Descending="@Model.Request.Descending"
                                           asp-route-PageNum="@Model.Request.PageNum"
                                           class="btn btn-sm btn-outline-primary">
                                            Details
                                        </a>
                                    </td>
                                </tr>
                            }
                        </tbody>
                    </table>
                </div>
            </div>
        </div>

        <nav aria-label="Purchase order pagination" class="mt-3">
            <ul class="pagination">
                <li class="page-item @(Model.Request.PageNum <= 1 ? "disabled" : "")">
                    @if (Model.Request.PageNum > 1)
                    {
                        <a class="page-link"
                           asp-page="./Index"
                           asp-route-Search="@Model.Request.Search"
                           asp-route-Status="@Model.Request.PurchaseOrderStatus"
                           asp-route-PageNum="@(Model.Request.PageNum - 1)"                  
                           asp-route-FromDate="@Model.Request.FromDate?.ToString("yyyy-MM-dd")"
                           asp-route-ToDate="@Model.Request.ToDate?.ToString("yyyy-MM-dd")"
                           asp-route-PageSize="@Model.Request.PageSize"
                           asp-route-SortBy="@Model.Request.SortBy"
                           asp-route-Descending="@Model.Request.Descending">
                            Previous
                        </a>
                    }
                    else
                    {
                        <span class="page-link">Previous</span>
                    }
                </li>
                @for (var pageNumber = 1; pageNumber <= Model.PurchaseOrders.TotalPages; pageNumber++)
                {
                    <li class="page-item @(pageNumber == Model.Request.PageNum ? "active" : "")">
                        <a class="page-link"
                           asp-page="./Index"
                           asp-route-Search="@Model.Request.Search"
                           asp-route-Status="@Model.Request.PurchaseOrderStatus"
                           asp-route-PageNum="@pageNumber"                    
                           asp-route-FromDate="@Model.Request.FromDate?.ToString("yyyy-MM-dd")"
                           asp-route-ToDate="@Model.Request.ToDate?.ToString("yyyy-MM-dd")"
                           asp-route-PageSize="@Model.Request.PageSize"
                           asp-route-SortBy="@Model.Request.SortBy"
                           asp-route-Descending="@Model.Request.Descending">
                            @pageNumber
                        </a>
                    </li>
                }

                <li class="page-item @(Model.Request.PageNum >= Model.PurchaseOrders.TotalPages ? "disabled" : "")">
                    <a class="page-link"
                       asp-page="./Index"
                       asp-route-Search="@Model.Request.Search"
                       asp-route-Status="@Model.Request.PurchaseOrderStatus"
                       asp-route-PageNum="@(Model.Request.PageNum + 1)"                      
                       asp-route-FromDate="@Model.Request.FromDate?.ToString("yyyy-MM-dd")"
                       asp-route-ToDate="@Model.Request.ToDate?.ToString("yyyy-MM-dd")"
                       asp-route-PageSize="@Model.Request.PageSize"
                       asp-route-SortBy="@Model.Request.SortBy"
                       asp-route-Descending="@Model.Request.Descending">
                        Next
                    </a>
                </li>
            </ul>
        </nav>
    }
</div>
